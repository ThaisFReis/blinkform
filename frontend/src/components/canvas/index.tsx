import GraphBuilder from '@/components/GraphBuilder';

export const Canvas = () => {
  return (
    <div className="w-full h-full relative overflow-hidden bg-[#08080C]">
      <GraphBuilder />
    </div>
  );
};
